# macOS
.DS_Store
.AppleDouble
.LSOverride

# Python
scripts/__pycache__/
*.pyc
*.pyo

# Temp output from the skill
/tmp/

# Local CE-RCA run outputs (may contain CE data) — never commit
CE RCA Runs/
runs/

# Internal planning (not for distribution)
thoughts/

# Never commit an access token if one lands in the tree
*.token
.ce-rca-token
